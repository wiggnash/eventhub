info:
  name: login
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}profiles/login/"
  body:
    type: json
    data: |-
      {
        "username": "wiggnash1",
        "password": "wiggnash1@123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
