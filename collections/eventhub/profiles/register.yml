info:
  name: register
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}profiles/register/"
  body:
    type: json
    data: |-
      {
        "username": "wiggnash1",
        "password": "wiggnash1@123",
        "email": "wiggnash@test.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
