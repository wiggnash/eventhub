info:
  name: Cancel a Reservations
  type: http
  seq: 5

http:
  method: POST
  url: "{{base_url}}reservations/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
