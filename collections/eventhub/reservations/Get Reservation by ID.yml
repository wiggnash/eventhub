info:
  name: Get Reservation by ID
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}reservations/1/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
