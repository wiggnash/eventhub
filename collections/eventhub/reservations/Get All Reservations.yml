info:
  name: Get All Reservations
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}reservations/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
