info:
  name: Update a reservations
  type: http
  seq: 4

http:
  method: PUT
  url: "{{base_url}}reservations/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
