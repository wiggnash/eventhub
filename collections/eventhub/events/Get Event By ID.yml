info:
  name: Get Event By ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}events/1/"
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
