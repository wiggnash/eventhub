info:
  name: Search By Venue
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}events/1/?venue=madras"
  params:
    - name: venue
      value: madras
      type: query
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
